import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    container:{
        bottom: 60,
        left: 0
    },
    progess:{
        borderRadius:20,
        marginHorizontal: 10
    }
})
export default styles